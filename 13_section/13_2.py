#カプセル化　１。オブジェクトによって複数の変数２。メソッドをまとめる事

class Data:
    def __init__(self):
        self.nums[index] = [1, 2, 3, 4, 5]
    
    def change_data(self, index, n):
        self.nums[index] = n

#クラス内にデータを隠蔽して外から見えないようにする。
