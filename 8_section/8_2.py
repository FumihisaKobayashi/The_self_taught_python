
#statisticsは平均、中央、最頻を計算して返す。
import statistics

#mean(平均値)
nums = [1, 5, 33, 12, 46, 33, 2]
print(statistics.mean(nums))

#median(中央値)
print(statistics.median(nums))

#mode(最頻値)
print(statistics.mode(nums))

